services:
  backend:
    build:
      context: ./backend

    restart: unless-stopped

  frontend:
    build:
      context: ./frontend

    ports:
      - "8080:80"

    depends_on:
      - backend

    restart: unless-stopped
